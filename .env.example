# POEditor MCP Configuration

# POEditor API Token (required)
# Get this from your POEditor account settings: https://poeditor.com/account/api
POEDITOR_API_TOKEN=your_poeditor_api_token_here

# Default POEditor project ID (optional)
# You can override this when calling MCP tools
POEDITOR_PROJECT_ID=your_project_id_here

# Default source language (optional, defaults to 'en')
POEDITOR_SOURCE_LANG=en

# Default target languages (optional, comma-separated)
POEDITOR_TARGET_LANGS=de,es,fr,it

# Rate limiting configuration (optional)
# Minimum delay between API requests in milliseconds
POEDITOR_RATE_LIMIT_DELAY=20000

# Batch size for bulk operations (optional)
POEDITOR_BATCH_SIZE=100

# Debug mode (optional, set to 'true' to enable verbose logging)
DEBUG=false

# Working directory for file operations (optional, defaults to current directory)
WORK_DIR=./

# Cache directory for storing temporary data and checksums (optional)
CACHE_DIR=./.smartness-i18n-cache

# Default configuration file path (optional)
CONFIG_FILE=.smartness-i18n.json